export { run } from "@oclif/command"

// const { execp } = require("./utils/execPromisified");

// let options = {
//   cwd: "/home/milan/projects/personal/gitsync",
// };

// execp("ls", options)
//   .then(console.log)
//   .catch((error) => {
//     console.error(`${error.type}: ${error.message}`);
//     console.log(`command: ${JSON.stringify(error.command)}`);
//     console.log(`options: ${JSON.stringify(error.options)}`);
//   });
